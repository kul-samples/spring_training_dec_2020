server:
    port: 1010    

spring:
  application:
    name: config-server
    
  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vatsanTraining/spring-cloud-config
          clone-on-start: true
 
          
          
management:
   endpoints:
    web:
      exposure:
        include:
        - "*"