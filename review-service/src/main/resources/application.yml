

spring:
  application:
    name: hospital-review-service
  config:
    activate:
      on-profile:
      - "dev"
    
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

      
      
      
server:
  port: 5050
  
---
  

spring:
  application:
    name: hospital-review-service
  config:
    activate:
      on-profile:
      - "prod"
    
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 5555
  